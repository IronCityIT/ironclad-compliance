{
  "name": "ironclad-compliance-rules-tests",
  "description": "firestore.rules exercised against the emulator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "firebase --project ironclad-compliance-test emulators:exec --only firestore \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.29.1"
  }
}
