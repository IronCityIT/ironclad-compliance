"""Assessment capabilities — one per file, discovered by ironclad.registry.

Adding a capability means adding a file here. Nothing else needs to change: the
CLI, the group presets and the dashboard's checkboxes all come from the registry
catalog, so a new module appears in every surface at once.
"""
